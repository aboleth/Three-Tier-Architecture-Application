# Three-Tier-Architecture-Application
This is a three-tier application in C#. It's a store's app template that adds, modifies and deletes customers and products using the SQL Server as Database. The code it's not only written in english, some parts of the code are written in brazilian portuguese like the variables and methods names, MessageBox texts and etc...
